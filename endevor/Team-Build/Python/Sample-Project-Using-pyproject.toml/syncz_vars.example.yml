---

# copy/paste this file to syncz_vars.yml
# update with valid values
# NEVER commit to git, which is why it has been added to .gitignore

remote_home: <path-to-dir>    # /z/user42
remote_host: <hostname>       # sys1.example.com
remote_pypi: <path-to-pypi>   # /opt/python/pypi
                              # https://pip.pypa.io/en/latest/cli/pip_install/#cmdoption-f

ssh_user: <userid>            # user42
ssh_keyfile: <ssh-key-file>   # /home/user42/.ssh/sys1_rsa
                              # https://www.ssh.com/academy/ssh/keygen
